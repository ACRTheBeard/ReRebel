# Capture Session — 2026-09-05-01 (docs inspection)

Method: read-only inspection of the playable copy's folder and bundled docs.
The game itself was NOT run (Windows exe, this shell is headless WSL).
No binaries, art, or audio were copied — filenames and facts only.

## Setup

- Playable copy: `Star Wars - Rebellion` folder on the user's Windows desktop.
- Game identity: Star Wars Rebellion, LucasArts 1998 (Steam AppID 441550
  per install script). Fan-patched setup: Update 1.02 + dgVoodoo2 DirectX
  wrapper + DirectPlay legacy enablement — i.e. it runs on modern Windows.
- Sources read: `README.TXT` (v1.0, 2/25/98), `README.DOC` + `TSGUIDE.DOC`
  (via strings), `TSGUIDE.TXT`, launch/install scripts. `Manual.pdf`
  (167 pp) is scanned images with no text layer — not yet mined.

## Media

- Video file: none yet.
- Screenshots: none yet.

## File inventory (structural only)

- `REBEXE.EXE` — main executable. `STRATEGY.DLL` / `TACTICAL.DLL` —
  strategy layer and tactical-battle modules (naming only, not reversed).
- `EData/`, `GData/`, `MDATA/` — game data dirs. `SWREditor/` — bundled
  scenario editor. `SaveGame/` — saves dir.
- `ENCYBMAP.DLL`, `ENCYTEXT.DLL`, `TEXTSTRA.DLL`, `TEXTTACT.DLL`,
  `TEXTCOMM.DLL` — encyclopedia/text resources (not extracted).
- `EMSPRITE.DLL` / `ALSPRITE.DLL`, `EMBRIEF*` / `ALBRIEF*` — per-side
  sprite/briefing resources (not extracted).
- `VOICEFXA.DLL` / `VOICEFXE.DLL` — per-side voice resources (not extracted).

## Inputs observed

Full strategic + tactical keyboard map transcribed to `input-map.md`
(source: README.TXT "Keyboard Commands"). Docs-derived, NOT yet verified
against live play — flagged as such in that file.

## Behaviors & rules noticed

Seeded into `gameplay-inventory.md` with per-row sources (all paraphrased
from README gameplay notes; manual still unread). Highlights:

- Two-layer game: strategic galaxy management + pausable real-time tactical
  battles (`Battle Paused` status text; observe-battle mode exists).
- Hyperspace travel with per-ship hyperdrive ratings; damage slows ships.
- Production economy with refined-material gating and progress-loss on
  quantity changes.
- Blockade → assault/bombard flow for capturing planets.
- Death Star as a special tactical weapon with charge/fire controls.
- Personnel with missions, Force progression, and Leadership-gated ranks.
- Multiplayer over TCP/IP exists (DirectPlay era) — v1 scope question.

## IP finding (feeds step-2 triage)

All title, ship, character, and music IP is Star Wars franchise
(Lucasfilm/Disney). Working assumption: NOTHING ships — every name, hull,
character, and cue must be replaced or licensed. Full triage stays in step 2.

## Open questions / still needed

- Live play observation or 10–20 min unedited video (core loop, screens,
  timings, feel numbers) — the manual + a play session would unlock the
  inventory's core-loop, difficulty, and UI-flow sections.
- Screenshots of every distinct screen/state.
- Manual.pdf contents (needs OCR or a manual read-through by the user).
- Confirm the Steam/1.02 build's rules match the 1998 manual (errata noted
  in README suggest drift).
- Min PC spec / first console decision still with the user.
